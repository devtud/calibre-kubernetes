apiVersion: v1
kind: PersistentVolumeClaim
metadata:
    name: calibre-books
    namespace: calibre
spec:
    accessModes:
        - ReadWriteOnce
    resources:
        requests:
            storage: 1Gi
    storageClassName: calibre
    volumeName: calibre-books
